% @dom.title  = "th3 sUpR Index !"
<!-- site navigation -->
<header>
</header>
% articles = Dir.glob("pages/articles/**/*.adoc")
% articles.each do |article|
<%= 
  component(
    "article_preview", OpenStruct.new({
      :article_path => article
    })
  )
%>
% end
