% @dom.title  = "0xcc bl0g"
<header>
  <!-- site navigation -->
</header>
% articles = Dir.glob("pages/articles/**/*.adoc")
% articles.each do |article|
<%= 
  component(
    "article_preview", {
      :article_path => article
    }
  )
%>
% end
